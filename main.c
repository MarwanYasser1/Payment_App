#include <stdio.h>
#include "Card.h"
#include "Terminal.h"

int main() {
	
	/*int i = 0;
	while (i == 0) {*/
		appStart();
	//	printf("Enter 0 to make another transaction, 1 to end: ");
	//	scanf_s("%d", &i);
	//}
	return 0;
}